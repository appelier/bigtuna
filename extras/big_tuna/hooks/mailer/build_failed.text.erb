The build for project <%= @project.name %> failed!

Commit that failed the build:
Hash: <%= @build.display_name %>
Author: <%= @build.author %> <<%= @build.email %>>
Message: <%= @build.commit_message %>

Link to build:
<%= build_url(@build) %>
